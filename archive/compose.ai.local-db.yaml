# Combine after compose.local-db.yaml and compose.ai.yaml on Linux. Schemii
# remains on the default bridge network, so the private OpenCode service needs
# no host publication or endpoint override.
services: {}
