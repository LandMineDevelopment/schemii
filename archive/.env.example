# Optional tutorial PostgreSQL settings only. Metadata and OpenCode credentials
# are never configured here; the launchers manage instance-scoped secret files.
# Copy this file to .env before first launch when changing the tutorial database.
SCHEMII_POSTGRES_DB=schemii
SCHEMII_POSTGRES_USER=schemii
SCHEMII_POSTGRES_PASSWORD=schemii-local
