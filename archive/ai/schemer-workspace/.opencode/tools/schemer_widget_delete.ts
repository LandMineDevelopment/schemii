import { tool } from "/opt/opencode/node_modules/@opencode-ai/plugin/dist/index.js"
export default tool({ description: "Propose deleting one exact widget while preserving every unrelated widget.", args: { dashboardId: tool.schema.string().min(1).max(128), expectedRevision: tool.schema.number().int().min(0), widgetId: tool.schema.string().min(1).max(128), currentTitle: tool.schema.string().trim().min(1).max(128) }, async execute() { return "Proposal arguments received." } })
