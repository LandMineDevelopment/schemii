import { tool } from "/opt/opencode/node_modules/@opencode-ai/plugin/dist/index.js"
export default tool({ description: "Propose duplicating one exact widget and appending the copy to dashboard order.", args: { dashboardId: tool.schema.string().min(1).max(128), expectedRevision: tool.schema.number().int().min(0), widgetId: tool.schema.string().min(1).max(128), currentTitle: tool.schema.string().trim().min(1).max(128), title: tool.schema.string().trim().min(1).max(128) }, async execute() { return "Proposal arguments received." } })
